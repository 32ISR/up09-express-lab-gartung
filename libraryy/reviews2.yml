info:
  name: reviews2
  type: http
  seq: 11

http:
  method: GET
  url: "{{base_url}}api/books/:id/reviews"
  params:
    - name: id
      value: "4"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
