info:
  name: books4
  type: http
  seq: 8

http:
  method: PUT
  url: "{{base_url}}api/books/:id"
  params:
    - name: id
      value: "5"
      type: path
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
