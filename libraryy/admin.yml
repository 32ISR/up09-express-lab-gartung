info:
  name: admin
  type: http
  seq: 13

http:
  method: GET
  url: "{{base_url}}api/admin/users"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
