info:
  name: login2
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}api/auth/login"
  body:
    type: json
    data: |-
      {
        "username": "admin",
        "password": "qwerty123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
